// Write C progrm to find the Size of int, float, char, double.
#include<stdio.h>
int main()
{
	int n;
	float f;
	char ch;
	double d;
	printf("The Size of int : %d",sizeof(n));
    printf("\nThe Size of float : %d",sizeof(f));
    printf("\nThe Size of char : %d",sizeof(ch));
    printf("\nThe Size of double : %d",sizeof(d));
}
